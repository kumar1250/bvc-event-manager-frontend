# Used by `npm run build:production` (vite build --mode production).
# Point this at your production Django deployment(s). Add a second,
# comma-separated URL to VITE_API_BASE_URLS for automatic failover.
VITE_API_BASE_URLS=https://bvce.onrender.com/api/v2
